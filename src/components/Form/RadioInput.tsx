import {View, Text} from 'react-native';
import React from 'react';
import {RadioButton} from 'react-native-paper';

const RadioInput = () => {
  return (
    <View>
      <RadioButton value="lal" />
    </View>
  );
};

export default RadioInput;
